# Fixtures

This directory is reserved for integration and translation fixture projects.

## Import Fixtures

- Translation tests currently generate temporary CMake fixtures during unit test execution.
- Keep reusable fixture projects here for stable integration scenarios that need a fixed directory layout.
