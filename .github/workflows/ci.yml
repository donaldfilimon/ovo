name: CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
        zig-version: ['0.16.0-dev.2984+cb7d2b056']

    steps:
    - uses: actions/checkout@v4

    - name: Setup Zig
      uses: goto-bus-stop/setup-zig@v2
      with:
        version: ${{ matrix.zig-version }}

    - name: Verify Zig version
      run: zig build zig-version-consistency

    - name: Typecheck
      run: zig build typecheck

    - name: Unit tests
      run: zig build unit-tests

    - name: CLI smoke tests
      run: zig build cli-tests-smoke

    - name: CLI help matrix
      run: zig build cli-help-matrix

  full-check:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Setup Zig
      uses: goto-bus-stop/setup-zig@v2
      with:
        version: '0.16.0-dev.2984+cb7d2b056'

    - name: Full check
      run: zig build full-check
